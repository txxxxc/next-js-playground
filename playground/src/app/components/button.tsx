'use client'

export const Button = (
  // propsでイベントを受け取る
  // props: { onClick: () => void; type: 'submit' | 'button' }
) => {
  {/* <button onClick={props.onClick} type="button"> */ }
  return (
    <button>
      Login
    </button >
  )
}

